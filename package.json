{
  "name": "egg-request-curl",
  "eggPlugin": {
    "name": "RequestCurl"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^2.1.0",
    "eslint": "^5.0.0",
    "lint-staged": "^7.1.1"
  },
  "version": "1.3.0",
  "description": "Egg plugin to log HTTP/HTTPS requests as curl for debugging",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "precommit": "lint-staged"
  },
  "author": "chenjingnan@jd.com",
  "license": "Apache",
  "engines": {
    "node": ">= 8.6.0",
    "npm": ">= 5.6.0"
  },
  "lint-staged": {
    "*.{js}": [
      "eslint --fix",
      "git add"
    ]
  },
  "dependencies": {
    "node_request_curl": "^2.0.0"
  }
}
