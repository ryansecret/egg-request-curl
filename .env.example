# HTTP 请求 curl 转换器 - 环境变量配置示例
# 将此文件复制为 .env 并根据需要修改

# =====================================================
# 请求 curl 转换器配置
# =====================================================

# 响应体大小限制（字节，默认3072）
# 超过此限制时，响应体会被替换为占位符文本
# 开发环境建议: 3072-5120
# 生产环境建议: 1024-2048
NODE_REQUEST_CURL_RESPONSE_LIMIT=3072

# 请求体大小限制（字节，默认2048）
# 超过此限制时，请求体不会包含在 curl 命令中
# 开发环境建议: 2048-4096
# 生产环境建议: 512-1024
NODE_REQUEST_CURL_REQUEST_LIMIT=2048

# 响应体过大时的占位符文本
# 默认值: "the response is too large"
# 可设置为中文: "响应数据过大"
NODE_REQUEST_CURL_SIZE_MESSAGE="the response is too large"

# 默认字符编码（默认utf8）
NODE_REQUEST_CURL_ENCODING=utf8

# 是否启用请求日志记录
# 可选值: true(启用) | false(禁用)
# 默认值: true
ENABLE_REQUEST_LOGGING=true

# =====================================================
# 日志配置
# =====================================================

# 日志目录路径
# 如不设置，将使用默认路径
# LOG=/export/Logs

# =====================================================
# Node.js 环境配置
# =====================================================

# 应用运行环境
# 可选值: development | test | production
NODE_ENV=production

# 临时忽略 SSL 证书验证（仅开发环境，生产禁用）
# NODE_TLS_REJECT_UNAUTHORIZED=0

# =====================================================
# 配置优先级说明
# =====================================================
#
# 配置参数的优先级从高到低为：
# 1. 函数参数配置 - app.js 中的 recorderOptions
# 2. 环境变量配置 - 本文件中的配置
# 3. 配置文件配置 - config/config.default.js
# 4. 默认值 - 代码中定义的默认值
#
# 例如，要覆盖响应体限制：
# - 通过环境变量: NODE_REQUEST_CURL_RESPONSE_LIMIT=5120
# - 通过 app.js: { lengthLimit: 5120, ... }
#
